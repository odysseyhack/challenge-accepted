import React, { Component } from 'react';
import { connect } from 'react-redux';

function mapStateToProps(state: any) {
    return {

    };
}

class InspectContract extends Component<any, any> {
    render() {
        return (
            <div>
                
            </div>
        );
    }
}

export default connect(
    mapStateToProps,
)(InspectContract);